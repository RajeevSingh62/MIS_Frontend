import type { Middleware } from '@reduxjs/toolkit';

// Placeholder: add custom middleware here as the app grows
// e.g. import loggerMiddleware from './loggerMiddleware';

const customMiddleware: Middleware[] = [
  // loggerMiddleware,
];

export default customMiddleware;
